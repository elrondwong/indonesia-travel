# 甘南 + 莲宝叶则下单决策闸门

生成时间：2026-06-25 22:46  
订单复核报告：[甘南莲宝叶则_订单复核报告.md](甘南莲宝叶则_订单复核报告.md)  
候选评分卡：[甘南莲宝叶则_候选评分卡.md](甘南莲宝叶则_候选评分卡.md)  
来源故障处理：[甘南莲宝叶则_来源故障处理.md](甘南莲宝叶则_来源故障处理.md)

这份闸门把出行信息、候选评分、行程覆盖、证据、复核日志和来源故障合成一个下单前 go/no-go 判定。它不会替代人工下单，只负责防止缺证据、缺覆盖或来源冲突时误下单。

## 当前结论

- 结论：不能下单。
- 阻塞闸门：6 个。
- 评分阈值：真实 shortlist/booked/backup 候选建议不低于 70 分。

| 闸门 | 状态 | 说明 |
|---|---|---|
| 出行信息 | 阻塞 | 缺：出发城市、出发日期、返程日期、人数、房间数、交通偏好：包车/自驾/待比较、每间夜酒店预算、摄影器材、可接受日均车程 |
| 候选表结构 | 阻塞 | 还没有真实候选 CSV |
| 候选评分 | 阻塞 | 没有可评分的真实活跃候选 |
| 行程覆盖 | 阻塞 | 没有真实候选，无法证明去返程、D1-D8 酒店、车辆和核心门票覆盖 |
| 证据归档 | 阻塞 | 还没有真实证据引用 |
| 复核日志 | 阻塞 | 缺少 data/gannan_reverification_log.csv |
| 来源故障 | 需人工确认 | 未执行在线来源探测；下单前应运行 --online-sources |

## 分项下单候选

| 类型 | 模式 | 候选数 | 当前最高分候选 | 分数 | 等级 |
|---|---|---:|---|---:|---|
| 大交通 | 模板 | 0 | 待填 | - | 待填候选 |
| 酒店 | 模板 | 0 | 待填 | - | 待填候选 |
| 包车/租车 | 模板 | 0 | 待填 | - | 待填候选 |
| 门票 | 模板 | 0 | 待填 | - | 待填候选 |

## 下一步动作

- 先用 `gannan-input-wizard.html` 生成 `data/gannan_order_inputs.json`。
- 复制并填写四类真实候选 CSV，至少覆盖去返程、D1-D8 酒店、车辆和核心门票。
- 运行 `python3 scripts/gannan_candidate_scorecard.py`，把低分候选补齐证据、退改/取消和关键确认。
- 运行 `python3 scripts/check_gannan_order_coverage.py --strict`，补齐缺失夜晚、车辆或核心门票来源。
- 把订单页截图、电话/聊天确认、公告截图和现场牌示存入 `evidence/gannan/` 并填回 CSV。
- 按出发日期生成并填写 `data/gannan_reverification_log.csv`，每轮写清状态、决策和下一步。
- 运行 `python3 scripts/run_gannan_checks.py --online-sources`，对 A 级失败来源按故障处理报告补人工证据。

## 使用命令

```bash
python3 scripts/gannan_booking_gate.py
python3 scripts/gannan_booking_gate.py --strict
python3 scripts/gannan_booking_gate.py --check
```

真实订单阶段完整检查：

```bash
python3 scripts/run_gannan_checks.py --actual
python3 scripts/run_gannan_checks.py --online-sources --actual
```
